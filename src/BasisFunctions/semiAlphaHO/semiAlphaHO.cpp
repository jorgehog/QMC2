/* 
 * File:   semiAlphaHO.cpp
 * Author: jorgehog
 * 
 * Created on 27. juni 2012, 13:17
 */

#include "../../QMCheaders.h"


HO_1::HO_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_2::HO_2(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_3::HO_3(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_4::HO_4(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_5::HO_5(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_6::HO_6(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_7::HO_7(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_8::HO_8(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_9::HO_9(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_10::HO_10(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_11::HO_11(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_12::HO_12(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_13::HO_13(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_14::HO_14(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

HO_15::HO_15(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_1_0::dell_HO_1_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_1_1::dell_HO_1_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_1::lapl_HO_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_2_0::dell_HO_2_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_2_1::dell_HO_2_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_2::lapl_HO_2(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_3_0::dell_HO_3_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_3_1::dell_HO_3_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_3::lapl_HO_3(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_4_0::dell_HO_4_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_4_1::dell_HO_4_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_4::lapl_HO_4(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_5_0::dell_HO_5_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_5_1::dell_HO_5_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_5::lapl_HO_5(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_6_0::dell_HO_6_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_6_1::dell_HO_6_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_6::lapl_HO_6(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_7_0::dell_HO_7_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_7_1::dell_HO_7_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_7::lapl_HO_7(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_8_0::dell_HO_8_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_8_1::dell_HO_8_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_8::lapl_HO_8(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_9_0::dell_HO_9_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_9_1::dell_HO_9_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_9::lapl_HO_9(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_10_0::dell_HO_10_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_10_1::dell_HO_10_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_10::lapl_HO_10(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_11_0::dell_HO_11_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_11_1::dell_HO_11_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_11::lapl_HO_11(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_12_0::dell_HO_12_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_12_1::dell_HO_12_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_12::lapl_HO_12(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_13_0::dell_HO_13_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_13_1::dell_HO_13_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_13::lapl_HO_13(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_14_0::dell_HO_14_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_14_1::dell_HO_14_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_14::lapl_HO_14(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_15_0::dell_HO_15_0(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

dell_HO_15_1::dell_HO_15_1(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

lapl_HO_15::lapl_HO_15(double* alpha, double w) {
    this->w = w;
    this->alpha = alpha;
}

//END OF CONSTRUCTORS






double HO_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 1;
    
    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_2::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * x;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_3::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * y;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_4::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * x * x - 2;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_5::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * y * y - 2;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_6::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * x*y;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_7::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * x * (2 * y * y - 1);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_8::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * y * (2 * x * x - 1);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_9::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 8 * y * y * y - 12 * y;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_10::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 8 * x * x * x - 12 * x;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_11::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 8 * y * x * (2 * x * x - 3);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_12::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 8 * y * x * (2 * y * y - 3);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_13::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 16 * y * y * (y * y - 3) + 12;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_14::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 16 * x * x * (x * x - 3) + 12;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double HO_15::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * (2 * x * x - 1)*(2 * y * y - 1);

    return H * exp(-0.5 * (*alpha) * w * r2);
}


double dell_HO_1_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -w * (*alpha) * x;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_1_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -w * (*alpha) * y;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = w * (*alpha) * (w * (*alpha) * r2 - 2);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_2_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * (1 - (*alpha) * w * x * x);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_2_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -2 * (*alpha) * w * x*y;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_2::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * x * w * (*alpha) * (w * (*alpha) * r2 - 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_3_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -2 * (*alpha) * w * y*x;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_3_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * (1 - (*alpha) * w * y * y);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_3::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * y * w * (*alpha) * (w * (*alpha) * r2 - 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_4_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * x * (4 + (*alpha) * w - 2 * x * x * (*alpha) * w);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_4_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = (4 * x * x - 2)*(-(*alpha) * w * y);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_4::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * (4 + w * (*alpha) * (2 - 12 * x * x + w * (*alpha) * r2 * (2 * x * x - 1)));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_5_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = (4 * y * y - 2)*(-(*alpha) * w * x);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_5_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * y * (4 + (*alpha) * w - 2 * y * y * (*alpha) * w);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_5::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 2 * (4 + w * (*alpha) * (2 - 12 * y * y + w * (*alpha) * r2 * (2 * y * y - 1)));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_6_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * y - 4 * x * x * y * w*(*alpha);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_6_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * x - 4 * x * y * y * w*(*alpha);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_6::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * (*alpha) * w * x * y * ((*alpha) * w * r2 - 6);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_7_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * (-1 + (*alpha) * w * x * x)*(-1 + 2 * y * y);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_7_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * x * y * ((*alpha) * w * (2 * y * y - 1) - 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_7::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * x * ((*alpha) * (*alpha) * w * w * r2 * (2 * y * y - 1) + 4 * (*alpha) * w * (1 - 4 * y * y) + 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_8_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * x * y * ((*alpha) * w * (2 * x * x - 1) - 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_8_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * (-1 + (*alpha) * w * y * y)*(-1 + 2 * x * x);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_8::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * y * ((*alpha) * (*alpha) * w * w * r2 * (2 * x * x - 1) + 4 * (*alpha) * w * (1 - 4 * x * x) + 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_9_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * (*alpha) * w * y * (3 - 2 * y * y) * x;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_9_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * (y * y * ((*alpha) * w * (3 - 2 * y * y) + 6) - 3);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_9::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * y * ((*alpha) * (*alpha) * w * w * r2 * (2 * y * y - 3) + 4 * (*alpha) * w * (3 - 4 * y * y) + 12);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_10_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * (x * x * ((*alpha) * w * (3 - 2 * x * x) + 6) - 3);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_10_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * (*alpha) * w * x * (3 - 2 * x * x) * y;

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_10::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * x * ((*alpha) * (*alpha) * w * w * r2 * (2 * x * x - 3) + 4 * (*alpha) * w * (3 - 4 * x * x) + 12);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_11_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -8 * y * ((*alpha) * w * x * x * (2 * x * x - 3) - 3 * (2 * x * x - 1));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_11_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -8 * x * (2 * x * x - 3)*((*alpha) * w * y * y - 1);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_11::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 8 * x * y * ((*alpha) * (*alpha) * w * w * r2 * (2 * x * x - 3) + 2 * (*alpha) * w * (9 - 10 * x * x) + 12);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_12_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -8 * y * (2 * y * y - 3)*((*alpha) * w * x * x - 1);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_12_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -8 * x * ((*alpha) * w * y * y * (2 * y * y - 3) - 3 * (2 * y * y - 1));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_12::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 8 * x * y * ((*alpha) * (*alpha) * w * w * r2 * (2 * y * y - 3) + 2 * (*alpha) * w * (9 - 10 * y * y) + 12);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_13_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * (*alpha) * w * x * (4 * y * y * y * y - 12 * y * y + 3);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_13_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * y * (3 * ((*alpha) * w + 8) + 4 * (*alpha) * w * y * y * y * y - 4 * y * y * (3 * (*alpha) * w + 4));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_13::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * ((*alpha) * (*alpha) * w * w * r2 * (3 + 4 * y * y * (y * y - 3)) + 2 * (*alpha) * w * (36 * y * y - 3 - 20 * y * y * y * y) + 24 * (y * y - 1));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_14_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * x * (3 * ((*alpha) * w + 8) + 4 * (*alpha) * w * x * x * x * x - 4 * x * x * (3 * (*alpha) * w + 4));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_14_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * (*alpha) * w * y * (4 * x * x * x * x - 12 * x * x + 3);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_14::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * ((*alpha) * (*alpha) * w * w * r2 * (3 + 4 * x * x * (x * x - 3)) + 2 * (*alpha) * w * (36 * x * x - 3 - 20 * x * x * x * x) + 24 * (x * x - 1));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_15_0::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * x * (2 * y * y - 1)*((*alpha) * w * (2 * x * x - 1) - 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double dell_HO_15_1::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = -4 * y * (2 * x * x - 1)*((*alpha) * w * (2 * y * y - 1) - 4);

    return H * exp(-0.5 * (*alpha) * w * r2);
}

double lapl_HO_15::eval(const Walker* walker, int i) {
    double r2 = walker->get_r_i2(i);

    double x = walker->r(i, 0);
    double y = walker->r(i, 1);

    double H = 4 * ((*alpha) * (*alpha) * w * w * r2 * (4 * x * x * y * y + 1 + 2 * r2) - 2 * (*alpha) * w * (1 + 6 * r2 + 20 * x * x * y * y) + 8 * (r2 + 1));

    return H * exp(-0.5 * (*alpha) * w * r2);
}

